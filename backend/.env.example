# If this backend runs on the same PC as Postgres, keep DB_HOST=localhost.
# If you run the backend on your Mac instead, use DB_HOST=100.117.157.38.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=PathFinder_db
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here
API_HOST=0.0.0.0
API_PORT=5000
